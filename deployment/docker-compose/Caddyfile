{$DOMAIN:localhost} {

    handle_path {$BACKEND_ROOT_PATH}* {
        reverse_proxy core_backend:8000
    }

    handle_path {$NEXT_PUBLIC_FRONTEND_ROOT_PATH}* {
        reverse_proxy front_end:3000
        rewrite * /aam/admin{path}
    }
}
